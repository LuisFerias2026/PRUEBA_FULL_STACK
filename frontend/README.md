# Frontend Angular — FlashReserve

La UI de reserva vive aquí. Instrucciones de ejecución, API y flujo de demo: [README raíz](../README.md).

```bash
npm install
npm start
```

Proxy a `http://localhost:8080`. Tests: `npm test`.
